// test registering a user

const User = require('../models/user.model')

const newUser = {
  username: 'test_user',
  email: 'test_email@test.com',
  isAdmin: false
}

describe('User', () => {
  describe('User can successfully register with correct credentials', async () => {
    
  })
})